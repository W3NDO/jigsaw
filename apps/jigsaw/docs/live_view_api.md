# LiveView API
When working with tiling, at the moment the only algorithm implemented is Binary Space Partitioning(BSP)

We build on top of liveview with named slots. More specifically, `pane` to define a pane
```

```
